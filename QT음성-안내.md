# 🔊 QT '오늘의 말씀 듣기' 음성 — 교회 PC 가 만듭니다

홈페이지에서 성도님이 **오늘의 말씀 듣기**를 누르면, 그 QT 음성을 **교회 당회실 PC**가 만들어 올립니다.
(2026-08-19 부터. 그전에는 집 PC 가 만들고 있었습니다.)

## 어떻게 도는가

1. 성도님이 버튼을 누름 → 홈페이지가 `tts_requests` 에 "이 날짜 음성 만들어 주세요" 를 접수
2. 교회 PC 의 **요청 워커**가 20초 안에 집어감 → GPT-SoVITS(로컬 AI 음성)로 낭독 생성
3. 만든 음성을 Cloudflare R2 에 `qt-<날짜>-<지문>.wav` 로 올림
4. 홈페이지가 그 파일을 재생 (다음부터는 즉시 재생, 다시 만들지 않음)

교회 PC 에는 워커가 **두 개** 돕니다.

| 워커 | 하는 일 | 로그 |
|---|---|---|
| `qt_request_worker.mjs` | 버튼을 누르면 그 즉시 생성 (20초마다 확인) | `C:\qt-video\qt-tts-daily\request.log` |
| `qt_tts_loop.mjs` | 새 QT 가 올라오면 미리 생성 (15분마다 확인) | `C:\qt-video\qt-tts-daily\daily.log` |

## 지킴이 (자동으로 되살아납니다)

두 워커는 창 없이 도는 프로그램이라, 누가 `node` 를 종료하면 **조용히 멈춥니다.**
실제로 2026-08-16 저녁에 멈춰 사흘간 집 PC 가 대신 만들고 있었습니다.

그래서 **지킴이**를 두었습니다 — 꺼져 있으면 **10분 안에 자동으로 다시 켭니다.**

- 작업 스케줄러 `QT-TTS-Watchdog` : 10분마다 점검
- 시작프로그램 `운평QT음성지킴이.vbs` : 로그인하자마자 점검
- 점검 기록 : `C:\qt-video\qt-tts-daily\watchdog.log`

## 목사님이 쓰실 파일 (tools 폴더)

| 파일 | 언제 쓰나 |
|---|---|
| `QT음성워커_상태.bat` | **지금 잘 돌고 있나** 확인할 때 (교회 PC) |
| `QT음성워커_설치.bat` | 교회 PC 를 새로 깔았거나 지킴이가 빠졌을 때 |
| `QT음성워커_끄기.bat` | **집 PC 에서** QT 음성 작업을 그만두게 할 때 |

전부 더블클릭만 하면 되고, 관리자 권한은 필요 없습니다.

## 집 PC 를 그만두게 하려면

집 PC 에서 `tools\QT음성워커_끄기.bat` 을 **한 번 더블클릭**하세요.
돌고 있던 워커를 끄고, 자동시작에서도 뺍니다. (되돌리려면 `QT음성워커_설치.bat`)

직접 하시려면: `Win+R` → `shell:startup` → **운평QT요청워커.vbs · 운평QT음성.vbs · 운평QT음성지킴이.vbs** 삭제
→ 작업관리자에서 `node.exe` 종료.

> 집 PC 를 켜 두어도 큰 문제는 없습니다. 두 PC 가 같은 요청을 동시에 집어도 한쪽만 처리하도록
> 되어 있고, 이미 만들어진 음성은 건너뜁니다. 다만 "교회 PC 가 만든다"로 정리하시려면 끄는 편이 깔끔합니다.

## 음성이 안 만들어질 때 확인 순서

1. `QT음성워커_상태.bat` 더블클릭 → **요청 워커 / 상시 루프 / GPT-SoVITS** 세 줄이 모두 `[정상]` 인지
2. `GPT-SoVITS` 가 `[꺼짐]` 이면 → `C:\GPT-SoVITS-v2pro-20250604\run_api.bat` 실행 (이게 꺼져 있으면 음성이 안 만들어집니다)
3. 그래도 안 되면 `C:\qt-video\qt-tts-daily\request.log` 마지막 줄을 확인
4. 교회 PC 가 꺼져 있으면 음성은 만들어지지 않습니다. 홈페이지는 12분 기다린 뒤
   **기본(브라우저) 음성**으로 읽어 주므로, 성도님 화면이 멈추지는 않습니다.

## 손대면 안 되는 것

`js/main.js` 의 낭독 텍스트 조립(`WPCQtText`)·지문 계산(`textSig`)을 바꾸면
교회 PC 의 `qt_tts_daily.mjs` 복제본도 **똑같이** 바꿔야 합니다.
안 그러면 파일 이름(지문)이 어긋나 이미 만든 음성을 못 찾고 매번 새로 만듭니다.
