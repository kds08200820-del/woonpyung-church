# 🎧 오디오북 — «레위기에서 만난 예수 그리스도»

책 한 권(원고 15만 자)을 **29개 트랙**(여는 글 · 1~27장 · 닫는 글, 약 6시간 20분)으로
낭독해 홈페이지에서 들려줍니다. **교적 인증을 마친 정회원은 무료**, 그 밖에는 잠깁니다.

목소리는 교회가 이미 쓰고 있는 QT·성경 낭독 음성(GPT-SoVITS · `C:\qt-video\assets\ref_voice.wav`)과
같은 목소리입니다. 비용은 0원이고 전부 교회 PC에서 만들어집니다.

---

## 한눈에 보는 구조

```
원고(.docx)
   │  python tools/audiobook_split.py
   ├─→ tools/_audiobook_scripts/book-NN.txt   ← 낭독 대본(저장소에 안 올림)
   └─→ js/audiobook-data.js                   ← 홈페이지 목차(제목·길이만)

낭독 대본
   │  node tools/audiobook_tts.mjs --all      ← GPT-SoVITS 로 합성
   └─→ Cloudflare R2 : audiobook/book-NN.mp3

듣기
   audiobook.html → 정회원 확인 → 워커가 6시간짜리 열쇠 발급 → 재생
```

---

## 처음 한 번만

1. **Cloudflare 워커 갱신** — `cloudflare/worker.js` 를 대시보드에 붙여넣고 Deploy
2. **비밀값 추가** — Settings → Variables and Secrets → Secret
   `BOOK_AUDIO_SECRET` = 24자 이상 무작위 문자열
   (자세한 방법은 `cloudflare/설정가이드.md` 아래쪽 '오디오북 추가 설정')

이 두 가지를 하기 전에는 오디오북 페이지가 "설정이 아직 끝나지 않았습니다"로 뜹니다.
다른 기능은 영향받지 않습니다.

---

## 음원 만들기 (교회 PC)

GPT-SoVITS API를 먼저 켭니다: `C:\GPT-SoVITS-v2pro-20250604\run_api.bat`

```bash
node tools/audiobook_tts.mjs --all
```

- 이미 올라간 장은 **건너뜁니다**. 중간에 끊겨도 같은 명령을 다시 실행하면 이어서 합니다.
- 한 장만: `node tools/audiobook_tts.mjs --ch 3`
- 여러 장: `node tools/audiobook_tts.mjs --ch 0-5`
- 다시 만들기: `--force`
- 올리지 않고 시험만: `--out _audiobook_out`

전체 합성에 **1시간 30분쯤** 걸립니다(장당 2~7분).

### 원고를 고쳤다면
```bash
python tools/audiobook_split.py
```
를 먼저 실행해 대본과 목차를 새로 만든 뒤, 바뀐 장만 `--ch N --force` 로 다시 만드세요.

---

## 관리 — 목회행정 대시보드

**목회행정 → 📚 오디오북 음원 관리** 카드

- `29/29` 처럼 몇 개가 올라와 있는지 보여 줍니다
- 카드를 누르면 장별로 **미리듣기 · 용량 · 올린 날짜 · 삭제**
- 아직 없는 장은 회색으로 "아직 없음"

---

## 성도가 보는 화면

**말씀으로 → 오디오북 듣기** (또는 홈 화면 책 소개, 책 미리보기 페이지의 버튼)

| 상태 | 보이는 것 |
|---|---|
| 로그인 안 함 | 🔒 로그인 안내 |
| 준회원 | 🔒 정회원 안내 + '내 정보로 이동'(교적 연결) |
| 정회원·관리자 | 목차 29개 · 재생기 |

재생기에는 이전/다음 장, 1·1.25·1.5배속, 이어듣기(이 기기에 위치 저장),
장을 다 들으면 ✓ 표시가 있습니다.

---

## 왜 이렇게 막아 두었나

이 책은 부크크에서 **판매 중**입니다. 홈페이지 미리보기(`book.html`)는 1장까지만 공개하고,
오디오북은 27장 전체를 담고 있어 공개되면 책이 통째로 풀립니다. 그래서

- 음원은 공개 주소 `…/f/audiobook/…` 로 **항상 거부**됩니다
- 재생은 정회원 확인을 거친 **6시간짜리 서명**이 붙은 주소로만 됩니다
- 주소에는 이름·아이디 같은 개인정보를 넣지 않습니다(파일 이름과 만료 시각만 서명)
- 낭독 대본(`tools/_audiobook_scripts/`)은 **저장소에 올라가지 않습니다**(.gitignore)

완벽한 복제 방지는 아닙니다(들리는 소리는 녹음될 수 있습니다).
검색·링크로 새어 나가는 것을 막고, 정회원이라는 울타리를 두는 정도입니다.

---

## 자주 겪는 문제

| 증상 | 원인·해결 |
|---|---|
| "오디오북 설정이 아직 끝나지 않았습니다" | 워커에 `BOOK_AUDIO_SECRET` 미설정 |
| 대시보드 카드가 `–` | 워커 재배포 안 됨, 또는 관리자 권한 없음 |
| 재생 중 갑자기 멈춤 | 6시간 서명 만료 — 새로고침(자동 재발급도 시도함) |
| "GPT-SoVITS(9880)가 꺼져 있습니다" | `run_api.bat` 실행 후 다시 시도 |
| 정회원인데 잠김 | `member_links.member_status` 가 `정회원`인지 확인(교적 연결 필요) |
