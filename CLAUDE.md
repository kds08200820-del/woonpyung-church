# 운평장로교회 홈페이지 작업 규칙

## 절대 금지
- 프로덕션 Supabase에 직접 연결하는 스크립트 실행 금지
- DROP TABLE, DROP COLUMN, DELETE, TRUNCATE 실행 금지 (SQL 파일 작성은 가능, 실행은 사람이 함)
- 실제 교인 데이터(이름, 연락처, 헌금, 기도제목) 조회·수정·출력 금지
- .env.production 파일 열람·수정 금지
- `supabase db push --linked`, `supabase db reset --linked` 등 원격 대상 명령 실행 금지
- git push 직접 실행 금지 (커밋까지만, push는 사람이 함)

## 작업 순서 (반드시 이 순서)
1. 로컬 Supabase(supabase start)에서만 개발·테스트
2. 시드 데이터(supabase/seed.sql)로만 테스트 — 실제 데이터 사용 금지
3. 스키마 변경은 반드시 마이그레이션 파일로만 작성 (직접 SQL 실행 금지)
4. 로컬 테스트 통과 → 변경 요약 보고 → 사람이 검토 → 사람이 push

## 삭제 정책
- 칼럼/테이블을 없애야 할 때: DROP이 아니라 `_archived` 접미사로 RENAME만
- 실제 삭제는 최소 4주 후, 백업 확인 후, 사람이 직접 실행
- 프론트엔드에서 조회만 중단하는 방식을 우선 고려

## 마이그레이션 규칙
- 파일명: YYYYMMDD_HHMM_설명.sql
- 모든 마이그레이션에는 되돌리는 방법을 주석으로 명시
- ADD COLUMN은 반드시 nullable 또는 DEFAULT 값 지정
- 한 마이그레이션에 파괴적 변경과 비파괴적 변경을 섞지 말 것

## 보고 형식
작업 완료 시 반드시 아래를 보고:
- 변경한 파일 목록
- 스키마 변경 여부 (있으면 마이그레이션 파일 경로)
- 로컬 테스트 결과
- 사람이 해야 할 다음 행동
